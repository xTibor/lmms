if(LMMS_HAVE_OPENMPT)
	INCLUDE(BuildPlugin)
	INCLUDE_DIRECTORIES(${OPENMPT_INCLUDE_DIRS})
	LINK_DIRECTORIES(${OPENMPT_LIBRARY_DIRS})
	LINK_LIBRARIES(${OPENMPT_LIBRARIES})
    BUILD_PLUGIN(moduleimport ModuleImport.cpp ModuleImport.h)
endif(LMMS_HAVE_OPENMPT)
